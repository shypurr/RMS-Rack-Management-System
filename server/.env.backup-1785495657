# Local development. Copied from .env.example.
# Matches a default Homebrew `mysql@8.0` install: root user, no password.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=rms
PORT=4000
